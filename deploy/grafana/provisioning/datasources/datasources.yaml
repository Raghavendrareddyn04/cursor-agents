apiVersion: 1
datasources:
  - name: Prometheus
    uid: Prometheus
    type: prometheus
    access: proxy
    url: http://kube-prometheus-stack-prometheus.observability.svc.cluster.local:9090
    isDefault: true
    editable: false
  - name: SunnyProgress
    uid: SunnyProgress
    type: yesoreyeram-infinity-datasource
    access: proxy
    # Rajesh MUST replace REPLACE_DOMAIN with the real $DOMAIN from .env before `helm upgrade --install`.
    # A leftover placeholder makes the pipeline-progress panel show "No data".
    url: https://REPLACE_DOMAIN/progress.json
    jsonData:
      auth_method: none
      global_queries: []
    editable: true
